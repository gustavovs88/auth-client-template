import { memo } from "react";

import { ResetPassword } from "@pages/public/authentication/resetPassword/resetPassword/ResetPassword";

const ResetPasswordPage = memo(() => {
  return <ResetPassword />;
});

export default ResetPasswordPage;
